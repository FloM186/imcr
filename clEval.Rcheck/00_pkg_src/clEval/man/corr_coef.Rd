% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corr_coef.R
\name{corr_coef}
\alias{corr_coef}
\title{Compute correlation coefficient of quantitative variables and clusters}
\usage{
corr_coef(
  active_variables,
  clusters,
  show_graph = TRUE,
  show_conditionnal_means = TRUE,
  digits = 3
)
}
\arguments{
\item{active_variables}{a numeric vector, data frame or matrix}

\item{clusters}{a vector of same length as variables containing the clustering values}

\item{show_graph}{logical. If FALSE, doesn't return the graph}

\item{show_conditionnal_means}{logical. If false, doesn't return the graph}

\item{digits}{a number. Number of digits displayed in tables}
}
\value{
The squared correlation coefficient table (in console and html format displayed in Viewer tab in Rstudio) and a barplot with means
}
\description{
This function returns the squared correlation coefficient from quantitative variables and clusters
}
\examples{
data(iris)
iris.cr <- scale(iris[,-5],center=TRUE,scale=TRUE)
d.iris <- dist(iris.cr)
cah.ward <- hclust(d.iris,method="ward.D2")
groupes.cah <- cutree(cah.ward,k=4)
corr_coef(iris[,-5], groupes.cah, show_graph = TRUE, digits=3)

}
